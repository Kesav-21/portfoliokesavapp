import 'package:flutter/material.dart';

class CertCard extends StatelessWidget {
  const CertCard({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(),
    );
  }
}

class myCertifications {}
